parameters:
  - name: artifactName
    type: string
    default: 'drop'

stages:
  - stage: 'CI'
    displayName: 'Build'

    jobs:
      - template: ../jobs/python-build.yml
        parameters:
          artifactName: ${{ parameters.artifactName }}
