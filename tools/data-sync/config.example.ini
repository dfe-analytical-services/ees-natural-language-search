[database]
db_server_name = <Server name>
content_database_name = content
statistics_database_name = statistics

[azure_openai]
embedding_endpoint = https://api.education.gov.uk/openai/deployments/{model_name}/embeddings?api-version=2024-10-21
api_key = <Find and Use an API subscription key>
api_version = 2024-10-21

[filtering]
# Optional to allow restricting the output to specific publications and
# release versions. Leave these blank, or remove the section entirely,
# to process everything returned by the database queries.
#
# CSV list of publication IDs.
publication_ids =
# CSV list of release version IDs.
release_version_ids =